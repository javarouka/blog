<article id="<%= post.layout %>-<%= post.slug %>" class="article article-single article-type-<%= post.layout %>" itemscope itemprop="blogPost">
    <div class="article-inner">
        <% if (post.link || post.title) { %>
            <header class="article-header">
                <%- partial('post/title', { class_name: 'article-title' }) %>
            </header>
        <% } %>
        <% if (post.layout != 'page') { %>
            <div><%- partial('post/draft', { post: post }) %></div>
            <div class="article-subtitle">
                <%- partial('post/date', { class_name: 'article-date', date_format: null }) %>
                <%- partial('post/tag') %>
            </div>
        <% } %>
        <%- partial('post/gallery') %>
        <%- partial('post/repImage', { post: post }) %>
        <% if(post.draft) { %>
        <div class="toggle-article">
            <p>작성중입니다!</p>
            <a>작성중인 글이라도 보기</button>
        </div>
        <% } %>
        <div class="article-entry <%- (post.draft ? 'draft' : '') %>" itemprop="articleBody">
            <%- post.content %>
        </div>
        <footer class="article-footer">
            <%- partial('share/index', { post: post }) %>
        </footer>
    </div>
</article>
<%- partial('comment/index') %>
